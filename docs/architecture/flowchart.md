graph TD
    A[用户输入标的] --> B(数据加载模块)
    B --> C{数据校验}
    C -->|通过| D[策略生成引擎]
    C -->|失败| E[返回错误信息]
    D --> F[多维度评分]
    F --> G[策略排序]
    G --> H[风险管理系统]
    H --> I{风险通过?}
    I -->|是| J[生成推荐策略]
    I -->|否| G
    J --> K[结果展示] 